{
  "name": "mdex-singularity",
  "version": "6.0.0",
  "description": "MDex Singularity - Absolute Coverage & Performance",
  "workspaces": [
    "src/ui"
  ],
  "scripts": {
    "tauri": "tauri",
    "kernel": "powershell -Command \".\\venv\\Scripts\\python -m src.api.server\"",
    "dev": "npm run tauri -- dev",
    "build": "npm run tauri -- build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.5.0"
  }
}
